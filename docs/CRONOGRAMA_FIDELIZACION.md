# MVS Commerce — Cronograma de Fidelización

Punto permanente de control de fases del módulo de Fidelización.

**Fuente oficial del orden y la numeración de fases:** `docs/Cronograma_Maestro_Fidelizacion_MVS_Commerce_Actualizado_23-08-2026.xlsx` (hoja "Cronograma Fidelización", 45 fases + subfase F08.1).

> Este documento refleja el Excel maestro vigente a su fecha. Cualquier cambio del Excel debe reflejarse aquí. El Excel manda en ORDEN; Git + código + tests mandan en EVIDENCIA de lo implementado.

---

## Regla de interpretación

- **CRONOGRAMA (Excel maestro)** = orden oficial y numeración de fases.
- **ESTADO_ACTUAL** (`docs/ESTADO_ACTUAL.md`) = punto actual de trabajo / relevo.
- **GIT + CÓDIGO + TESTS** = qué está realmente implementado.
- **PROGRESO** (`docs/PROGRESO.md`) = resumen del avance.

Nunca utilizar únicamente uno de ellos.

Si cronograma y código difieren: **NO retroceder ni rehacer trabajo automáticamente**. Registrar la discrepancia y aplicar el protocolo de contradicción.

### Regla sobre trabajo adelantado

Si se descubre una funcionalidad implementada antes de su fase, esa fase se marca **COMPLETADO (ADELANTADO)** con su evidencia, pero eso **NO cambia cuál fase corresponde ejecutar a continuación** según el cronograma.

Ejemplo confirmado: **F28 está completado de forma adelantada durante la integración POS, pero eso NO convierte a F29 en la siguiente fase: la siguiente fase es F19 — Premios por puntos.**

---

## Regla obligatoria para agentes

Antes de trabajar en Fidelización:

1. leer `AGENTS.md`;
2. leer `docs/CRONOGRAMA_FIDELIZACION.md` (este archivo);
3. leer `docs/ESTADO_ACTUAL.md`;
4. leer la sección Fidelización de `docs/PROGRESO.md`;
5. comprobar `git status`;
6. revisar commits recientes relacionados;
7. inspeccionar código y tests de la fase;
8. determinar la fase real antes de modificar código.

Un agente NO puede:

- saltar fases silenciosamente;
- retroceder a una fase ya implementada;
- inventar una fase;
- renumerar fases;
- reinterpretar una fase;
- declarar completa una fase solo porque la documentación lo diga;
- cambiar el cronograma sin autorización del usuario.

Si existe contradicción entre fuentes:

> DETENER implementación → informar discrepancia → presentar evidencia → esperar decisión cuando afecte el orden del cronograma.

---

## Tabla maestra F01–F45

Estados tomados del Excel maestro del 23-08-2026. La columna "Evidencia" agrega la confirmación del repositorio (commits `8392dd4` — canje de puntos; `7be1f80` — integración POS; auditoría posterior: 152 tests Loyalty/POS-Loyalty sin fallos).

| ID | Etapa | Entregable | Estado | Prioridad | Depende de | Criterio de cierre | Evidencia |
|---|---|---|---|---|---|---|---|
| F01 | 1. Análisis | Revisión del proyecto | COMPLETADO | CRÍTICA | — | Informe técnico recibido; análisis sin modificaciones | análisis inicial |
| F02 | 1. Análisis | Arquitectura de Fidelidad | COMPLETADO | CRÍTICA | F01 | Arquitectura base aprobada: configuración, cuenta, Kardex, movimientos y líneas | diseño aprobado |
| F03 | 1. Análisis | Reglas funcionales finales | COMPLETADO | CRÍTICA | F01-F02 | Reglas aprobadas: porcentaje, promociones, bonos, canje, vencimiento, reversos y multisucursal | reglas aprobadas |
| F04 | 1. Análisis | Diseño visual | COMPLETADO | ALTA | F01 | Administración centralizada y portal cliente elegante, responsive, estilo MVS Commerce | diseño aprobado |
| F05 | 2. Base | Módulo y configuración | COMPLETADO | CRÍTICA | F02-F04 | Base implementada: tablas, modelos, restricciones, DECIMAL(19,4), Kardex y pruebas | `8392dd4`; `LoyaltyInfrastructureTest` |
| F06 | 2. Base | Cuenta de fidelidad | COMPLETADO | CRÍTICA | F05 | Cuenta y Kardex central con LoyaltyAccountService, transacciones, locks, BCMath, idempotencia y reversión | `8392dd4`; `LoyaltyAccountServiceTest` |
| F07 | 2. Base | Saldo y Kardex | COMPLETADO | CRÍTICA | F06 | Kardex administrativo de solo lectura con filtros, paginación, aislamiento por empresa y permiso `fidelidad.ver` | `8392dd4`; `LoyaltyKardexTest` |
| F08 | 3. Acumulación | Regla de acumulación por porcentaje | COMPLETADO | CRÍTICA | F07 | Monto elegible × porcentaje / 100, BCMath, redondeo half-up y auditoría | `8392dd4`; `LoyaltyEarningServiceTest` |
| F08.1 | 3. Acumulación | Porcentaje configurable | COMPLETADO | CRÍTICA | F08 | Cambiar porcentaje y comprobar que el cálculo cambia correctamente | `8392dd4`; `LoyaltyPercentageSettingTest` |
| F09 | 3. Acumulación | Cliente nuevo | COMPLETADO | ALTA | F01-F08 | Puntos acreditados una sola vez al completar venta | `8392dd4`; tipo `new_customer` en `LoyaltyMovement`/`LoyaltyAccountService` |
| F10 | 3. Acumulación | Cumpleaños | COMPLETADO | ALTA | F07-F08 | Bono correcto y no duplicado | `8392dd4`; `LoyaltyBirthdayServiceTest` |
| F11 | 3. Acumulación | Cliente que retorna | COMPLETADO | ALTA | F07-F08 | Regla se dispara correctamente | `8392dd4`; `LoyaltyReturningCustomerServiceTest` |
| F12 | 3. Acumulación | Multiplicadores | COMPLETADO | ALTA | F08 | Cálculo correcto en promoción | `8392dd4`; `LoyaltyMultiplierTest` |
| F13 | 3. Acumulación | Ofertas | COMPLETADO | ALTA | F08 | Configurar si acumula puntos en ofertas; regla respetada | `8392dd4`; `earn_on_offers`, `LoyaltyPosIntegrationTest` |
| F14 | 4. Canje | Valor del punto | COMPLETADO | CRÍTICA | F07 | Canje calcula valor correcto (1 punto = ₡1 por defecto, configurable) | `8392dd4`; `LoyaltyPointValueTest` |
| F15 | 4. Canje | Mínimo para usar puntos | COMPLETADO | CRÍTICA | F14 | Sistema bloquea/permite según configuración (ej. mínimo ₡3.000) | `8392dd4`; `LoyaltyRedemptionMinimumTest` |
| F16 | 4. Canje | Máximo de una compra | COMPLETADO | ALTA | F15 | POS respeta límite: % de la compra pagable con puntos | `8392dd4`; `LoyaltyRedemptionLimitTest` |
| F17 | 4. Canje | Usar puntos en ofertas | COMPLETADO | ALTA | F15-F16 | Regla respetada al canjear sobre ofertas | `8392dd4`; `redeem_on_offers`, `LoyaltyRedemptionServiceTest` |
| F18 | 4. Canje | Forma de pago Puntos | COMPLETADO | CRÍTICA | F14-F17 | Venta completa con puntos como forma de pago en POS | `7be1f80`; suite POS-Loyalty completa |
| F19 | 5. Premios | Premios por puntos | COMPLETADO | CRÍTICA | F14 | Crear, activar, desactivar y canjear premios directos | administración implementada (`loyalty_rewards`, `LoyaltyRewardController`, permiso `fidelidad.premios`, `LoyaltyRewardTest`); el canje de premios corresponde a F21 |
| F20 | 5. Premios | Stock / disponibilidad | COMPLETADO | ALTA | F19 | No permite canje de premio agotado | `availability_mode` (unlimited/limited/product) + `stock_quantity` + `product_id` en `loyalty_rewards`; consulta central en `LoyaltyRewardAvailabilityService`; `LoyaltyRewardAvailabilityTest` (7 tests). El consumo atómico (lock de cupo y descuento vía `InventoryPostingService`) corresponde al canje F21 |
| F21 | 5. Premios | Historial de canjes | COMPLETADO | CRÍTICA | F07,F19 | Kardex + canje coinciden | `loyalty_reward_redemptions` con snapshots; `LoyaltyRewardRedemptionService` (atómico, idempotente por `event_key`, cupo con `lockForUpdate`, inventario vía `InventoryPostingService` tipo `reward_redemption`); `LoyaltyMovement::TYPE_REWARD`; permiso `fidelidad.canjes`; `LoyaltyRewardRedemptionTest` (11 tests) |
| F22 | 6. Vencimiento | Vencimiento configurable | COMPLETADO | ALTA | F07 | Sí/No + meses libres de inactividad, sin opciones rígidas | reutilizados `expiration_enabled`/`expiration_months` existentes desde la base (sin migración); validación Sí/No + meses enteros libres 1–120 en `UpdateLoyaltySettingRequest`; UI en configuración de Fidelización; `LoyaltyExpirationSettingTest` (7 tests). La expiración automática corresponde a F23 |
| F23 | 6. Vencimiento | Vencimiento automático | COMPLETADO | ALTA | F22 | Salida de puntos trazable en Kardex al vencer | `LoyaltyExpirationService` + comando `loyalty:expire-points` + scheduler diario con `withoutOverlapping()`; idempotencia `expiration:{account_id}:{due_date}`, meses exactos sin overflow, saldo bajo lock nunca negativo, `total_expired` correcto; `LoyaltyExpirationTest` (13 tests); regresión Loyalty/POS-Loyalty 177 tests OK |
| F24 | 7. Bonos | Centro de reglas | COMPLETADO | CRÍTICA | F09-F13 | Cliente nuevo, cumpleaños, retorno y reglas futuras desde un solo lugar | "Centro de reglas" en Fidelización (`loyalty.rules.*`, permiso `fidelidad.configuracion`): edita la fila única `LoyaltySetting` de la empresa vía `UpdateLoyaltySettingRequest::toValues()` (validación compartida con la pantalla general; sin copia paralela) + accesos a multiplicadores/premios/canjes/Kardex; entrada nueva en sidebar. Nota: el bono "cliente nuevo" existe como tipo `new_customer` sin configuración propia; el centro lo deja listo para cuando se defina su política. Evidencia: `LoyaltyRuleCenterTest` (6 tests); regresión Loyalty/POS-Loyalty 193 tests OK |
| F25 | 7. Bonos | Ajuste manual | COMPLETADO | ALTA | F07 | Permiso + Kardex + motivo obligatorio | Ajustes de puntos (`loyalty.adjustments.*`, permiso nuevo `fidelidad.ajustes` sembrado): cliente + sumar/restar + puntos DECIMAL(19,4) + motivo obligatorio; usa `LoyaltyAccountService::adjustPoints` (`TYPE_ADJUSTMENT`), nunca edita saldo, bloquea saldo negativo; usuario/sucursal/empresa registrados; idempotencia HTTP por `event_key` `adjustment:{uuid}`; historial paginado en pantalla y movimiento visible en Kardex. Evidencia: `StoreLoyaltyAdjustmentRequest`, `LoyaltyAdjustmentController`, vista `loyalty/adjustments/index`; `LoyaltyManualAdjustmentTest` (10 tests) |
| F26 | 8. Multisucursal | Saldo global de empresa | COMPLETADO | CRÍTICA | F06-F08 | Compra en sucursales distintas suma al mismo saldo | Arquitectura ya global por diseño: cuenta única `(company_id, customer_id)` en `LoyaltyAccountService::getOrCreateAccount` (sin branch), `branch_id` solo como origen del movimiento; evidencia previa `LoyaltyPosIntegrationTest::test_customer_account_is_global_across_branches`; brecha de aislamiento cerrada con `LoyaltyMultiBranchTest` (empresas separadas no comparten saldo ni cuentas) |
| F27 | 8. Multisucursal | Canje en cualquier sucursal | COMPLETADO | CRÍTICA | F18,F26 | Canje cruzado entre sucursales activas | Verificado end-to-end sin cambios de código: canje POS HTTP en sucursal B consume saldo acumulado en A y registra `branch_id` B (`LoyaltyMultiBranchTest`); premios F21: cupo `limited` global por empresa (lock en fila del premio), modo `product` consulta/descuenta stock de la sucursal ejecutora (bloquea sin stock en B aunque A tenga); cross-company bloqueado (validación de contexto + tests existentes F21); idempotencia y atomicidad conservadas |
| **F28** | **9. Anulación** | **Reversión de puntos** | **COMPLETADO (ADELANTADO)** | CRÍTICA | F07,F18 | Puntos revertidos y trazables al anular venta | `7be1f80`; `SaleVoidService`, `SaleVoidLoyaltyTest` |
| F29 | 9. Devolución | Ajuste por devolución | COMPLETADO | CRÍTICA | F07,F28 | Pruebas de devolución pasan | `LoyaltySaleReturnAdjustmentService` integrado a la transacción de `SaleReturnService`: reversión proporcional de puntos ganados (base = `base_amount` elegible del earn original vs subtotal acumulado devuelto) y restauración proporcional de puntos canjeados (base = total de la venta vs total acumulado devuelto), BCMath escala 4 con redondeo half-up explícito y deltas acumulativos que nunca exceden lo original; idempotencia por `event_key` `sale:return:{id}:earned|redeemed`; saldo insuficiente bloquea la devolución atómicamente (sin negativos ni inconsistencias); Kardex con metadata auditable. `reverseMovement` generalizado para montos parciales sin romper F28. Evidencia: `SaleReturnLoyaltyTest` (9 tests, 79 aserciones); regresión 228 tests OK |
| F30 | 10. Portal | Portal del cliente | COMPLETADO | CRÍTICA | F19-F23 | Web responsive para puntos, premios, movimientos y promociones | `LoyaltyCustomerPortalService` (solo lectura, reutiliza `LoyaltyPointValueService`, Kardex y catálogos existentes) + `LoyaltyCustomerPortalController` + vista `loyalty/portal/show` con layout propio responsive mobile-first (`layouts/portal`); ruta `fidelidad/portal/{cliente}` (`loyalty.portal.show`) bajo `auth`+`active.company`+`permission:fidelidad.ver`; cliente resuelto siempre dentro de la empresa activa; saldo + valor monetario, historial paginado del cliente, premios activos de la empresa, promociones = multiplicadores vigentes (semántica F12); etiquetas de tipos centralizadas en `LoyaltyMovement::LABELS`; sin QR/PIN/enlaces ni identidad nueva (eso es F31–F35). Evidencia: `LoyaltyCustomerPortalTest` (7 tests, 51 aserciones); regresión Loyalty/POS-Loyalty/Devoluciones: 244 tests, 1609 aserciones, 0 fallos |
| F31 | 10. Portal | Identidad visual | COMPLETADO | ALTA | F04,F30 | Logo/nombre de tienda, estilo único MVS Commerce | Reutilizados únicamente datos existentes de `Company` (`trade_name`, `logo` en disco público): encabezado del portal muestra logo vía la convención existente `asset('storage/'.$company->logo)` con alt "Logo de {nombre}" (misma que `empresa/edit`) y fallback elegante con inicial del nombre comercial cuando no hay logo; sin columnas nuevas, sin temas por empresa, estilo único MVS Commerce (slate/ámbar); título del documento incluye el nombre comercial. Evidencia: `LoyaltyCustomerPortalTest::test_portal_shows_own_brand_with_logo_or_elegant_fallback`; regresión portal+Loyalty: 246 tests, 1624 aserciones, 0 fallos |
| F32 | 10. Portal | Marca MVS Commerce | COMPLETADO | MEDIA | F30 | Pie discreto "Hecho con MVS Commerce" | Pie `<footer>` discreto y legible en `layouts/portal` ("Hecho con MVS Commerce", texto plano, consistente en móvil y escritorio); sin enlace porque no existe URL oficial configurada en el proyecto (no se inventa). Evidencia: `LoyaltyCustomerPortalTest::test_portal_footer_shows_mvs_commerce_brand_discretely` (verifica presencia del pie y ausencia de enlace); regresión portal+Loyalty: 246 tests, 1624 aserciones, 0 fallos |
| F33 | 10. Portal | QR | **COMPLETADO** | ALTA | F30,F34 | QR funciona y no expone información indebida | Generación local con `chillerlan/php-qrcode` 6.0.1 (única dependencia autorizada; transitiva `chillerlan/php-settings-container` 3.3.0). El QR codifica exactamente el enlace seguro F34 (`loyalty.portal.access`): un solo mecanismo de tokens, sin customer_id público ni datos personales; salida SVG vectorial (ECC H, viewBox responsive) apta para impresión. `LoyaltyPortalAccessService::qrSupported()`/`qrSvg()`: el QR se entrega junto al enlace en la única respuesta donde el token existe en claro y NUNCA se persiste (BD conserva solo el hash SHA-256); regenerar o revocar invalida automáticamente cualquier QR impreso anterior porque su enlace deja de resolver. Sin APIs externas de QR (prohibidas: enviarían el token a terceros). Personal autorizado (`fidelidad.portal`) genera/regenera, copia enlace, ve e imprime el QR. Evidencia: `LoyaltyPortalAccessQrTest` nuevo (5 tests) + `LoyaltyPortalAccessTest` actualizado (7); regresión Loyalty/POS-Loyalty/Devoluciones/Portal: 259 tests, 1749 aserciones, 0 fallos |
| F34 | 10. Portal | Acceso por enlace | COMPLETADO | ALTA | F30 | Link seguro funcional | Tabla `loyalty_portal_accesses` + `LoyaltyPortalAccess` + `LoyaltyPortalAccessService`: token aleatorio 60 chars (CSPRNG `Str::random`) asociado a `(company_id, customer_id)`, almacenado SOLO como hash SHA-256 (visible una vez al generar/regenerar), regeneración revoca el anterior, revocación explícita, `last_used_at`, ruta pública `/fidelidad/portal/acceso/{token}` (`loyalty.portal.access`, `throttle:30,1`, sin auth staff) que resuelve token→empresa+cliente activos y renderiza el mismo portal F30–F32 vía `LoyaltyCustomerPortalService`; administración "Accesos al portal" (`loyalty.accesses.*`) bajo permiso nuevo sembrado `fidelidad.portal` con sidebar condicionado; URL sin IDs internos ni datos personales; cross-company imposible (el token define el contexto). Evidencia: `LoyaltyPortalAccessTest` (7 tests, 70 aserciones); regresión Loyalty/POS-Loyalty/Devoluciones/Portal: 253 tests, 1696 aserciones, 0 fallos |
| F35 | 10. Portal | Publicidad/promociones | **COMPLETADO** | MEDIA | F30 | Contenido visible y administrable | Tabla `loyalty_promotions` + `LoyaltyPromotion` + `LoyaltyPromotionService`: contenido promocional administrable por empresa (título, descripción corta opcional, inicio/fin datetime UTC convertidos desde la zona horaria empresarial, activo/inactivo, orden simple `sort_order`), independiente de los multiplicadores F12 que conservan su sección y semántica propias. Vigencia inclusiva (`is_active && starts_at <= ahora <= ends_at`) con la misma semántica temporal/zona horaria de F12; orden por prioridad. Administración CRUD mínimo (`loyalty.promotions.index/store/update/toggle`) bajo permiso nuevo sembrado `fidelidad.promociones` (Administrador automático) con sidebar condicionada, badge de estado (vigente/futura/vencida/inactiva) y 404 ante empresas ajenas. Portal público F30–F34 muestra sección nueva "Promociones vigentes" solo con promociones activas y vigentes de la empresa del token/cliente, estado vacío elegante, responsive mobile-first. Decisiones V1: sin sucursal (portal resuelve empresa global, puntos globales F26) y sin imagen (no existe infraestructura de uploads reutilizable segura para este módulo; se evita inventar una). Evidencia: `LoyaltyPromotionTest` (6 tests); regresión Loyalty/POS-Loyalty/Devoluciones/Portal: 265 tests, 1804 aserciones, 0 fallos |
| F36 | 11. Online | Acumulación online | **COMPLETADO** | CRÍTICA | F08,F30 | Compra online suma al mismo saldo | Capa mínima sobre venta real confirmada: `LoyaltyOnlineSaleService::accrueForSale(Sale, ?Customer, referenciaExterna, canal)` reutiliza F08/F12/F13 y bonos F10/F11 sin duplicar lógica; misma cuenta central `(company_id, customer_id)` (sin cuentas web paralelas); idempotencia `online_sale:{canal}:{ref}:loyalty:earn` contra índice único `(company_id, event_key)` — reintentos no duplican; origen auditado en metadata del movimiento (`channel/origin/external_reference`) + `source_type=Sale`; valida venta completed, empresa activa y sucursal válida; bloquea cliente ajeno (validación central); sin cliente no acredita (comportamiento actual); no toca inventario. Sin columnas nuevas ni tienda/API (no existía ecommerce previo). Evidencia: `LoyaltyOnlineSaleTest` (11 tests); regresión 276 tests, 1846 aserciones, 0 fallos |
| F37 | 11. Online | Canje online | **COMPLETADO** | CRÍTICA | F15-F18,F36 | Canje online descuenta saldo central | `LoyaltyOnlineSaleService::redeemForSale(Sale, ?Customer, puntosSolicitados, referenciaExterna, canal)`: reutiliza `LoyaltyRedemptionService::redeem` sin duplicar reglas — F14 valor del punto, F15 mínimo de saldo, F16 máximo pagable con puntos sobre el total de la venta, F17 `redeem_on_offers`, precisión decimal e idempotencia por `event_key` determinista `online_sale:{canal}:{ref}:loyalty:redemption` (independiente del earn F36). Pago con puntos representado como en POS: `SalePayment` con el `PaymentMethod` tipo `loyalty_points` de la empresa (`affects_cash_snapshot=false`, `created_by=sale->user_id`), coordinado con el movimiento dentro de UNA transacción — fallo posterior al descuento revierte todo (probado inyectando fallo). Cliente obligatorio; venta no confirmada/empresa inactiva bloquean; cliente ajeno bloqueado por validación central; reintentos no duplican movimiento ni pago; sin inventario. Evidencia: `LoyaltyOnlineRedemptionTest` (12 tests); regresión Loyalty/POS-Loyalty/Devoluciones/Portal: 288 tests, 1907 aserciones, 0 fallos |
| **F38** | **12. Permisos** | **Administrador** | **COMPLETADO** | CRÍTICA | F24-F25 | Permisos correctos para configurar todo | Auditoría completa: 13 permisos `fidelidad.*` sembrados y asignados al rol Administrador via wildcard sync; todas las rutas protegidas con `permission:fidelidad.*` middleware; sidebar condicionado correctamente; aislamiento por empresa verificado en los 10 controladores. `LoyaltyAdminAuthorizationTest` (28 tests, 88 aserciones): permisos sembrados, Admin recibe todos, acceso Admin a 12 rutas, 403 en 10 rutas sin permiso, sidebar completo, 2 tests de aislamiento cross-company. Regresión: Loyalty 295/295, POS-Loyalty 18/18, Portal 26/26, Sidebar 2/2, 0 fallos |
| **F39** | **12. Permisos** | **Cajero** | **COMPLETADO** | CRÍTICA | F18,F38 | Consulta/canje permitidos; 403 en configuración | Auditoría sin cambios de negocio: el perfil Cajero configurado con `pos.acceder` y `ventas.crear` ya consulta saldo por `pos.loyalty.summary` y canjea durante el checkout real; no requiere permisos administrativos `fidelidad.*`. Configuración, reglas, ajustes, multiplicadores, premios, canjes administrativos, accesos y promociones devuelven 403; sidebar no expone administración. El canje conserva empresa, sucursal y usuario en Kardex. Evidencia: `LoyaltyCashierAuthorizationTest` (3 tests, 22 aserciones); regresión `--filter Loyalty`: 298 tests, 1910 aserciones, 0 fallos |
| **F40** | **13. Dashboard** | **Indicadores** | **COMPLETADO** | MEDIA | F07,F21-F23 | Totales de clientes, generados, canjeados, vencidos y saldo | `LoyaltyDashboardIndicatorService` agrega por empresa la fuente de verdad `loyalty_accounts`: cantidad de cuentas, `total_earned`, `total_redeemed`, `total_expired` y `balance`, conservando escala decimal 4 y ceros explícitos sin cuentas. Dashboard muestra cinco tarjetas responsive y mantiene los indicadores operativos previos. Aislamiento empresarial y ausencia de contaminación cross-company probados; sin desglose por sucursal (F41). Evidencia: `LoyaltyDashboardIndicatorTest` (3 tests, 5 aserciones); regresión `--filter Loyalty`: 301 tests, 1915 aserciones, 0 fallos |
| **F41** | **13. Dashboard** | **Empresa / sucursal** | **COMPLETADO** | MEDIA | F26,F40 | Información global por empresa y por sucursal | F40 permanece como resumen global de cuentas. `LoyaltyDashboardIndicatorService::byBranch()` agrega actividad neta por `branch_id`: clientes con movimiento, puntos generados, canjeados y vencidos; incluye sucursales sin actividad y fila "Sin sucursal" para procesos globales/automáticos. Reversiones `return`/`void` se atribuyen según el tipo del movimiento original para no inflar acumulación o canje. El saldo no se reparte artificialmente: sigue rotulado global por empresa conforme F26. UI mobile-first con tarjetas <768px y tabla `overflow-x-auto` desde `md`. Evidencia: `LoyaltyDashboardIndicatorTest` (5 tests, 17 aserciones); regresión `--filter Loyalty`: 303 tests, 1927 aserciones, 0 fallos |
| F42 | 14. Calidad | Suite de pruebas | PENDIENTE | CRÍTICA | F01-F41 | Suite completa aprobada | nota: ya existe suite amplia Loyalty/POS-Loyalty (152 tests OK); quedan áreas de fases pendientes |
| F43 | 14. Calidad | UI / usabilidad | PENDIENTE | CRÍTICA | F30-F41 | Prueba visual/manual aprobada | — |
| F44 | 14. Calidad | Regresión | PENDIENTE | CRÍTICA | F42-F43 | Clientes, POS, Compras y Caja siguen funcionando | — |
| F45 | 15. Cierre | Respaldo GitHub | PENDIENTE | CRÍTICA | F44 | diff --check, tests, commit, push, working tree limpio | — |

---

## Control por etapas (resumen hoja 2 del Excel)

| Etapa | Objetivo | Estado |
|---|---|---|
| 1. Análisis y diseño | Mapa técnico + reglas + diseño aprobados | COMPLETADO |
| 2. Base | Configuración, cuenta y Kardex | COMPLETADO |
| 3. Acumulación | Porcentaje configurable + bonos + multiplicadores | COMPLETADO |
| 4. Canje | Puntos como pago + límites + ofertas | COMPLETADO |
| 5. Premios | Premios directos y canjes | COMPLETADO (F19-F21) |
| 6. Vencimiento | Inactividad + vencimiento automático | COMPLETADO (F22-F23) |
| 7. Administración | Centro de reglas y ajustes | COMPLETADO (F24-F25) |
| 8. Multisucursal | Saldo global por empresa | COMPLETADO (F26-F27) |
| 9. Reversiones | Anulación y devolución | COMPLETADO (F28 adelantado + F29) |
| 10. Portal cliente | Web + QR + promociones | **COMPLETADO** (F30–F35) |
| 11. Tienda online | Acumulación y canje online | **COMPLETADO** (F36–F37) |
| 12. Permisos | Administrador vs cajero | COMPLETADO (F38-F39) |
| 13. Dashboard | Empresa + sucursales | COMPLETADO (F40-F41) |
| 14. Calidad | Tests + UI + regresión | PENDIENTE |
| 15. Cierre | GitHub respaldado | PENDIENTE |

---

## Regla central de acumulación (hoja 3 del Excel)

- Modelo: el cliente **NO** recibe 1 punto por cada ₡1 comprado.
- Cálculo: `puntos = monto elegible × porcentaje de fidelización`.
- Ejemplo: compra ₡1.000 con 5% → ₡50 de beneficio → 50 puntos.
- Configuración: el administrador elige el porcentaje; nunca fijo en código.
- Canje inicial: 1 punto representa ₡1 de valor de canje, sujeto a las reglas configuradas.

---

## Contraste cronograma vs repositorio (discrepancias registradas)

Fases que el maestro mantiene PENDIENTES pero donde código/tests ya muestran avance parcial o total. **No cambiar su estado oficial sin autorización del usuario; registrar y esperar decisión:**

- **F26 — Saldo global de empresa**: RESUELTO — el usuario autorizó cerrar F26; ver fila de la tabla maestra. La evidencia previa (`LoyaltyPosIntegrationTest::test_customer_account_is_global_across_branches`) quedó complementada con `LoyaltyMultiBranchTest`.
- **F27 — Canje en cualquier sucursal**: RESUELTO — verificado y cerrado junto con F26 mediante `LoyaltyMultiBranchTest` (canje POS cruzado, cupo global, stock por sucursal ejecutora).
- **F25 — Ajuste manual**: soporte a nivel servicio existía (`LoyaltyAccountService`, ajustes en ambas direcciones con test); la interfaz administrativa con motivo obligatorio y permiso propio quedó implementada en F25 (COMPLETADO).
- **F42 — Suite de pruebas**: suite Loyalty/POS-Loyalty amplia ya existente y en verde; incompleta respecto a fases aún pendientes (premios, vencimiento).
- Brecha futura fuera del cronograma: WhatsApp registra contactos/plantillas pero no envía por API.

---

## Estado y próxima fase

- **Completadas:** F01–F41 según maestro (F28 completada de forma adelantada durante la integración POS, `7be1f80`). Etapas 10, 11, 12 y 13 completas.
- **F41 — Empresa / sucursal: COMPLETADO.** El dashboard conserva indicadores globales F40 y añade actividad neta por sucursal de origen: clientes con movimiento, generados, canjeados y vencidos; contempla reversiones enlazadas, sucursales sin actividad y movimientos sin sucursal. El saldo permanece global por empresa conforme F26, sin asignación ficticia. Presentación responsive con tarjetas móviles y tabla desktop contenida. Evidencia: `LoyaltyDashboardIndicatorTest` (5 tests, 17 aserciones); regresión `--filter Loyalty`: 303 tests, 1927 aserciones, 0 fallos.
- **F40 — Indicadores: COMPLETADO.** Servicio dedicado agrega directamente las cuentas de Fidelización de la empresa activa: clientes con cuenta, puntos generados, canjeados, vencidos y saldo vigente. Valores DECIMAL con escala 4, estado cero sin cuentas y aislamiento cross-company probado. La vista conserva el resumen operativo existente y añade cinco tarjetas mobile-first; F41 aportará el desglose empresa/sucursal. Evidencia: `LoyaltyDashboardIndicatorTest` (3 tests, 5 aserciones); regresión `--filter Loyalty`: 301 tests, 1915 aserciones, 0 fallos.
- **F39 — Cajero: COMPLETADO.** Auditoría confirmó que consulta y canje pertenecen al flujo POS: `pos.loyalty.summary` requiere `pos.acceder` y el checkout con canje requiere además `ventas.crear`; no corresponde conceder permisos administrativos `fidelidad.*` por nombre de rol, porque los roles son configurables por empresa. Prueba end-to-end con rol Cajero: consulta saldo y máximo, canje real con pago mixto, movimiento auditable por empresa/sucursal/usuario y saldo coherente; ocho áreas administrativas responden 403 y el sidebar no expone su navegación. Evidencia: `LoyaltyCashierAuthorizationTest` (3 tests, 22 aserciones); regresión `--filter Loyalty`: 298 tests, 1910 aserciones, 0 fallos.
- **F37 — Canje online: COMPLETADO.** Misma capa online de F36, ahora con canje: `redeemForSale()` reutiliza íntegramente `LoyaltyRedemptionService` (F14 valor del punto, F15 mínimo de saldo vía `LoyaltyRedemptionEligibilityService`, F16 máximo pagable sobre el total de la venta vía `LoyaltyRedemptionLimitService`, F17 `redeem_on_offers`) sin duplicar una sola regla; saldo central compartido con POS. El pago con puntos se registra exactamente como en POS (`SalePayment` + `PaymentMethod` tipo `loyalty_points`, sin efecto de caja) dentro de la MISMA transacción del movimiento: fallo posterior al descuento revierte todo (verificado inyectando un fallo en la creación del pago). Idempotencia independiente del earn (`...:loyalty:redemption`); reintentos no duplican movimiento ni pago. Cliente obligatorio; venta no confirmada/empresa inactiva/cliente ajeno bloqueados. Evidencia: `LoyaltyOnlineRedemptionTest` (12 tests); regresión 288 tests, 1907 aserciones, 0 fallos.
- **F36 — Acumulación online: COMPLETADO.** No existía infraestructura ecommerce previa (INTEGRACIONES §5 = futuro; D013 sin diseño aprobado), así que se implementó la capa mínima reutilizable: `LoyaltyOnlineSaleService::accrueForSale(Sale, ?Customer, externalReference, channel='online')`. Recibe una venta REAL confirmada (`status=completed`), resuelve empresa activa y sucursal válida desde la propia venta, y reutiliza exactamente el pipeline del POS sin duplicar reglas: monto elegible y `earn_on_offers` vía `LoyaltyOfferEligibilityService` (F13), porcentaje y precisión decimal vía `LoyaltyEarningService::earnFromEligibleAmount` (F08), multiplicadores vigentes vía `LoyaltyMultiplierResolver` (F12) y bonos F10/F11 con sus servicios propios. Idempotencia por `event_key` determinista `online_sale:{canal}:{referencia_externa}:loyalty:earn` sobre la restricción `(company_id, event_key)` de `loyalty_movements`: reintentos no duplican puntos ni bonos. Cuenta central `(company_id, customer_id)`: sin cuentas web paralelas; cliente ausente = sin acreditación (comportamiento actual). Origen online auditado en metadata (`channel`, `origin`, `external_reference`) y `source_type=Sale`; no toca inventario ni crea pedidos/tienda/API. Sin columnas nuevas de canal. Evidencia: `LoyaltyOnlineSaleTest` (11 tests); regresión Loyalty/POS-Loyalty/Devoluciones/Portal: 276 tests, 1846 aserciones, 0 fallos.
- **F35 — Publicidad/promociones: COMPLETADO.** Contenido promocional administrable por empresa visible en el portal: tabla `loyalty_promotions` (título, descripción corta opcional, `starts_at`/`ends_at`, `is_active`, `sort_order`; índice `(company_id, is_active, starts_at, ends_at)`), `LoyaltyPromotionService::vigentes()` centraliza la visibilidad (activa + vigente ahora, inclusiva, zona horaria de la empresa → UTC como F12, orden por prioridad). Administración CRUD mínimo bajo permiso nuevo sembrado `fidelidad.promociones` con badge de estado y aislamiento estricto (404 cross-company); portal público muestra sección "Promociones vigentes" independiente de los multiplicadores F12 (sección propia conservada) con estado vacío elegante. Decisiones V1: sin sucursal (portal resuelve empresa global; puntos globales F26) y sin imagen (sin infraestructura segura/reutilizable de uploads en el módulo). Evidencia: `LoyaltyPromotionTest` (6 tests); regresión 265 tests, 1804 aserciones, 0 fallos.
- **F33 — QR: COMPLETADO.** Generación local con `chillerlan/php-qrcode` 6.0.1 (estable, PHP ^8.2, PHP puro; única dependencia autorizada, arrastra transitivamente `chillerlan/php-settings-container` 3.3.0). El QR codifica únicamente el enlace seguro F34 — mismo mecanismo de tokens, sin segundo sistema ni IDs públicos ni datos personales. Salida SVG (ECC H, viewBox responsive) para impresión/visualización; se muestra junto al enlace en la única respuesta donde el token existe en claro y nunca se persiste (BD = solo hash SHA-256). Regenerar o revocar invalida automáticamente el QR impreso anterior (su enlace deja de resolver). Sin APIs externas de QR. Evidencia: `LoyaltyPortalAccessQrTest` (5 tests) + actualización de `LoyaltyPortalAccessTest`; regresión 259 tests, 1749 aserciones, 0 fallos.
- **F38 — Administrador: COMPLETADO.** Auditoría completa del sistema de permisos de Fidelización: los 13 permisos `fidelidad.*` están sembrados en `PermissionSeeder` y asignados al rol Administrador via wildcard sync (`syncWithoutDetaching` de todos los permisos activos). Todas las 28 rutas de Fidelización tienen middleware `permission:fidelidad.*` correcto (excepto la ruta pública `loyalty.portal.access` por diseño y `pos.loyalty.summary` que es intencional para POS). El sidebar muestra las 10 entradas condicionadas por permiso. Los 10 controladores de Fidelización scopean correctamente por `session('active_company_id')` con protección cross-company. Evidencia: `LoyaltyAdminAuthorizationTest` (28 tests, 88 aserciones); regresión Loyalty (295), POS-Loyalty (18), Portal (26), Sidebar (2): 0 fallos.
- **SIGUIENTE:** **F42 — Suite de pruebas** (etapa 14. Calidad).
- **F34 — Acceso por enlace seguro: COMPLETADO.** Un solo mecanismo de tokens para enlace y futuro QR: tabla `loyalty_portal_accesses` con hash SHA-256 del token (60 chars CSPRNG), asociado a `(company_id, customer_id)`; regenerar revoca el anterior; revocación explícita; el token en claro se muestra una sola vez en "Accesos al portal" (permiso nuevo `fidelidad.portal` sembrado). Ruta pública `/fidelidad/portal/acceso/{token}` (throttle, sin auth staff) resuelve empresa+cliente y renderiza el portal F30–F32 existente vía `LoyaltyCustomerPortalService`. La URL no contiene customer_id, identificación, teléfono ni email. Decision: NO se usaron signed URLs (`URL::temporarySignedRoute`) porque expondrían IDs internos en la URL; se prefirió entidad dedicada con hash por revocación/regeneración/auditoría. Evidencia: `LoyaltyPortalAccessTest` (7 tests, 70 aserciones); regresión 253 tests, 1696 aserciones, 0 fallos.
- **F31 — Identidad visual: COMPLETADO.** El portal muestra la identidad de la empresa usando únicamente datos existentes (`trade_name`, `logo`): logo con la convención ya soportada (`asset('storage/…')`, alt "Logo de {nombre}") y fallback elegante con la inicial del nombre comercial; estilo único MVS Commerce, sin temas por empresa ni columnas nuevas. Revisión manual pendiente para el detalle visual exacto (F43 cubre UI/usabilidad).
- **F32 — Marca MVS Commerce: COMPLETADO.** Pie discreto "Hecho con MVS Commerce" en `layouts/portal`, texto plano sin enlace (no existe URL oficial configurada en el proyecto), consistente en móvil y escritorio.
- **F30 — Portal del cliente: COMPLETADO.** Vista web responsive/mobile-first donde se consulta la fidelización de un cliente: saldo actual, valor monetario equivalente (reutiliza `LoyaltyPointValueService`, sin cálculos propios), historial paginado de movimientos (fecha, tipo con etiqueta legible, puntos firmados y referencia), premios activos de la empresa y promociones de fidelización existentes en la arquitectura (multiplicadores vigentes con semántica F12). Aislamiento estricto por `(company_id, customer_id)`: el cliente se resuelve dentro de la empresa activa y todo query queda acotado a esa pareja. Módulo inactivo oculta saldo/catálogo pero conserva el historial. Sin QR, PIN, magic links ni identidad nueva de cliente (F31–F35); el ensamblado de datos (`LoyaltyCustomerPortalService`) está preparado para que esas fases aporten el mecanismo de acceso. No modifica reglas de acumulación, canje, expiración, anulación ni devoluciones. Evidencia: `LoyaltyCustomerPortalTest` (7 tests, 51 aserciones); regresión Loyalty/POS-Loyalty/Devoluciones: 244 tests, 1609 aserciones, 0 fallos.
- **F29 — Ajuste por devolución: COMPLETADO.** Toda devolución total o parcial ajusta la fidelización dentro de la misma transacción de `SaleReturnService`. Lado ganado: proporción = subtotal neto acumuladamente devuelto / `base_amount` elegible del earn original (tope 1), delta sobre lo ya revertido; lado canjeado: proporción = total acumuladamente devuelto / total de la venta (tope 1), delta sobre lo ya restaurado. BCMath escala 4 con redondeo half-up explícito (`round4`); los bonos fijos no se revierten por no ser proporcionales a mercancía. Idempotencia doble: `event_key` único por devolución y tipo (`sale:return:{id}:earned|redeemed`) más deltas calculados desde el Kardex vía `related_movement_id`. Saldo insuficiente rechaza la devolución completa de forma atómica (sin saldo negativo ni inconsistencias). Evidencia: `LoyaltySaleReturnAdjustmentService`, `reverseMovement` generalizado con monto parcial, `SaleReturnLoyaltyTest` (9 tests, 79 aserciones); regresión Devoluciones+F28+Loyalty+POS-Loyalty: 228 tests, 1481 aserciones, 0 fallos.
- **F23 — Vencimiento automático: COMPLETADO.** Detalle completo en `docs/PROGRESO.md`. Evidencia: `LoyaltyExpirationTest` (13 tests, 78 aserciones); regresión Loyalty + POS-Loyalty verde.
- Auditoría de referencia: regresión Loyalty en verde tras F22 (134 tests) más POS-Loyalty (48 tests); configuración de vencimiento: 7 tests, 62 aserciones. Tras F26-F27: `LoyaltyMultiBranchTest` (5 tests, 40 aserciones); regresión Loyalty + POS-Loyalty: 198 tests, 1295 aserciones, 0 fallos.
